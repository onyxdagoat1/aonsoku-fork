# Required: Path to your music library (must match Navidrome's music folder)
MUSIC_PATH=/path/to/music

# Optional: Server port (default: 3001)
PORT=3001

# Optional: Upload temporary directory (default: ./uploads)
UPLOAD_PATH=./uploads

# Optional: Data storage directory (default: ./data)
DATA_PATH=./data

# Optional: Navidrome integration
NAVIDROME_URL=http://localhost:4533
NAVIDROME_USER=admin
NAVIDROME_PASSWORD=your_password
